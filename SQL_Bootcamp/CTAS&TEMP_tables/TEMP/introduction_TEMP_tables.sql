Temporary tables --? Stores intermediate results in temporary storage
                 --? within the database during the session.
                 --? The database will drop all temporary tables once
                 --? the session ends.
