DISTINCT --? Removes duplicates (repeated values), each value appears only once

SELECT DISTINCT
    country
FROM table;

-- Return unique list of all countries

SELECT DISTINCT
	COUNTRY
FROM customers;

COUNTRY
Germany
UK
USA
