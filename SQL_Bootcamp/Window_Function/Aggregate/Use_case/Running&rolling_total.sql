--? They aggregate sequence of members, and the aggregation is updated each time
--? a new member is added

--* Running total
--? Aggregate all values from the beginning up to the point without dropping off older data

--* Rolling total
--? Aggregate all values within a fixed time window
--? As new data is added, the oldest data point will be dropped
